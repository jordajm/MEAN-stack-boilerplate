(function() {
    'use strict';

    function wrapperService() {

        

        var service = {

        };

        return service;
    }

    wrapperService.$inject = [];


    angular.module('admin.components.wrapper')
        .factory('wrapperService', wrapperService);
})();
