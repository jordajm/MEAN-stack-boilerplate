(function() {
    'use strict';

    function appModel($rootScope, todosEvents) {


        
        var model = {

        };

        return model;
    }

    appModel.$inject = [];

    angular.module('display')
        .factory('displayAppModel', appModel);
})();
